import React from 'react';
import styled from '@emotion/styled';

const Header = ({titulo}) => {
    return ( 
        <header>
            <h1>{titulo}</h1>
        </header>
     );
}
 
export default Header;